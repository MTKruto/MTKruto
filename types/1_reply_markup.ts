import { InlineKeyboardButton } from "./0_inline_keyboard_button.ts";

export declare namespace ReplyMarkup {
  export interface InlineKeyboard {
    inlineKeyboard: InlineKeyboardButton[];
  }

  export interface ReplyKeyboard {
  }
}
